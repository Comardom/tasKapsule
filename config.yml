version: 3
name: "taskapsule"
binaryName: "taskapsule"
assetDir: "frontend/dist"
devServerUrl: "http://localhost:5173"
frontend:
  dir: "frontend"
  install:
    - pnpm install
  build:
    - pnpm run build
  dev:
    - pnpm run dev